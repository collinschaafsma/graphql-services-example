type User {
  id: ID! @unique
  email: String! @unique
  password: String!
  profileId: ID!
  createdAt: DateTime!
  updatedAt: DateTime!
}

type UserOrganization {
  id: ID! @unique
  userId: ID!
  organizationId: ID!
  createdAt: DateTime!
}
